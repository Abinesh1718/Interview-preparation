✅ How would you optimize a React application? 
• Use React.memo for component memoization 
• Implement useMemo for expensive calculations 
• Use useCallback to prevent unnecessary re-renders from callback prop changes 
• Code splitting with React.lazy and Suspense 
• Virtualize long lists with libraries like react-window 
• Avoid inline function definitions in render 
• Use production build for deployment 
• Implement server-side rendering if needed


✅ Optimized Code
import React from 'react';

const UserList = React.memo({ users, onUserSelect }) {
  console.log('UserList rendering');
  return (
    <ul>
      {users.map(user => (
        <li key={user.id} onClick={() => onUserSelect(user)}>
          {user.name}
        </li>
      ))}
    </ul>
  );
});

export default UserList;


✅ How would you implement protected routes in React Router? 

import { Route, Redirect } from 'react-router-dom'; 
function ProtectedRoute({ isAuthenticated, component: Component, ...rest }) { 
return ( 
<Route 
{...rest} 
render={(props) => 
isAuthenticated ? ( 
<Component {...props} /> 
) : ( 
<Redirect to="/login" /> 
) 
} 
/> 
); 
} 
// Usage: 
<ProtectedRoute 
path="/dashboard" 
isAuthenticated={user != null} 
component={Dashboard} 
/>

✅ Explain error boundaries in React 
error Boundaries in React are special components that catch JavaScript errors in their
child component tree, log them, and display a fallback UI without crashing the entire app.

They catch errors during:
Rendering
Lifecycle methods
Constructors of child components

Implementation :

class ErrorBoundary extends React.Component { 
constructor(props) { 
super(props); 
this.state = { hasError: false }; 
} 
static getDerivedStateFromError(error) { 
return { hasError: true }; 
} 
componentDidCatch(error, errorInfo) { 
logErrorToService(error, errorInfo); 
} 
render() { 
if (this.state.hasError) { 
return <h1>Something went wrong.</h1>; 
} 
return this.props.children; 
} 
} 
// Usage: 
<ErrorBoundary> 
<MyComponent /> 
</ErrorBoundary> 


// ✅ Coding Task: Create a custom hook for fetching data 

import { useState, useEffect } from 'react'; 
function useFetch(url, options = {}) { 
const [data, setData] = useState(null); 
const [loading, setLoading] = useState(true); 
const [error, setError] = useState(null); 
useEffect(() => { 
const fetchData = async () => { 
try { 
const response = await fetch(url, options); 
if (!response.ok) { 
throw new Error(`HTTP error! status: ${response.status}`); 
} 
const json = await response.json(); 
setData(json); 
} catch (err) { 
setError(err.message); 
} finally { 
setLoading(false); 
} 
}; 
fetchData(); 
return () => { 
// Optionally abort fetch if component unmounts 
}; 
}, [url]); // Re-run effect if URL changes 
return { data, loading, error }; 
} 
function UserProfile({ userId }) { 
const { data: user, loading, error } = useFetch(`/api/users/${userId}`); 
if (loading) return <div>Loading...</div>; 
if (error) return <div>Error: {error}</div>; 
return ( 
<div> 
<h1>{user.name}</h1> 
<p>Email: {user.email}</p> 
</div> 
); 
}


✅  Explain the difference between lists, tuples, sets, and dictionaries in Python  ?
• Lists: Ordered, mutable, allows duplicates. [1, 2, 3] 
• Tuples: Ordered, immutable, allows duplicates. (1, 2, 3) 
• Sets: Unordered, mutable, no duplicates. {1, 2, 3} 
• Dictionaries: Unordered, mutable, key-value pairs, keys must be unique. {'a': 1, 'b': 2}

✅  What are Python decorators? Provide an example 
Python decorators are functions that modify the behavior of 
ther functions or methods without changing their code. 
They are often used for logging, authentication, caching, etc.

def my_decorator(func):
    def wrapper():
        print("Before the function runs")
        func()
        print("After the function runs")
    return wrapper

@my_decorator
def say_hello():
    print("Hello!")

say_hello()


✅ Coding Task: Write a Python function to find the first non-repeating character in a 
string ?

def first_non_repeating_char(s):
    freq = {}
    for char in s:
        freq[char] = freq.get(char, 0) + 1

    for char in s:
        if freq[char] == 1:
            return char
    return None 

result = first_non_repeating_char("abinesh")
print("First non-repeating character:", result)

✅ Explain Django's MTV architecture ?

  Django follows MTV (Model-Template-View) pattern, similar to MVC: 
• Model: Represents data structure (database tables) 
• Template: Handles presentation layer (HTML) 
• View: Business logic that processes requests and returns responses

✅  What is the difference between select_related and prefetch_related in Django ORM? 

select_related: 
• Used for foreign key and one-to-one relationships 
• Performs SQL JOIN to get related objects in single query 
• Better for "forward" relationships 

prefetch_related: 
• Used for many-to-many and reverse foreign key relationships 
• Performs separate lookup for each relationship 
• Better for "reverse" relationships and many-to-many 


✅  Coding Task: Create a Django model for a blog post with comments 

from django.db import models

class BlogPost(models.Model):
    title = models.CharField(max_length=200)
    content = models.TextField()
    author = models.CharField(max_length=100)
    created_at = models.DateTimeField(auto_now_add=True)

    def __str__(self):
        return self.title

class Comment(models.Model):
    blog = models.ForeignKey(BlogPost, on_delete=models.CASCADE, related_name='comments')
    name = models.CharField(max_length=100)
    comment = models.TextField()
    created_at = models.DateTimeField(auto_now_add=True)

    def __str__(self):
   return f'Comment by {self.name} on {self.blog}'


✅ What are the main components of Django REST Framework? 

• Serializers: Convert complex data types to/from Python datatypes 
• Views: API views (APIView, GenericAPIView, ViewSets) 
• Routers: For automatic URL routing with ViewSets 
• Authentication: Various auth methods (Token, Session, JWT) 
• Permissions: Control access to API endpoints 
• Pagination: Manage large result sets 
• Throttling: Control request rates 

✅ Coding Task: Create a simple DRF viewset for the BlogPost model 


views.py
from rest_framework import viewsets, permissions 
from .models import BlogPost 
from .serializers import BlogPostSerializer 

class BlogPostViewSet(viewsets.ModelViewSet): 
queryset = BlogPost.objects.filter(is_published=True) 
serializer_class = BlogPostSerializer 
permission_classes = [permissions.IsAuthenticatedOrReadOnly] 
def perform_create(self, serializer): 
serializer.save(author=self.request.user) 

# serializers.py 
from rest_framework import serializers 
from .models import BlogPost 
class BlogPostSerializer(serializers.ModelSerializer): 
author = serializers.StringRelatedField() 
class Meta: 
model = BlogPost 
fields = ['id', 'title', 'content', 'author', 'created_at', 'updated_at'] 
read_only_fields = ['author', 'created_at', 'updated_at'] 

✅  How would you optimize database queries in Django? 

• Use select_related and prefetch_related for related objects 
• Use only() and defer() to load only needed fields 
• Add proper database indexes 
• Use count() instead of len() for querysets 
• Avoid N+1 queries by batching requests 
• Use values() or values_list() when only specific fields are needed 
• Consider caching with Django's cache framework 
• Use iterator() for large querysets

✅  Optimize this view that has N+1 query problem 

Qustions : 

def author_posts(request): 
authors = Author.objects.all() 
return render(request, 'authors.html', { 
'authors': [{ 
'name': author.name, 
'posts': [p.title for p in author.post_set.all()] 
} for author in authors] 
}) 


Optimized Solution: 

def author_posts(request): 
authors = Author.objects.prefetch_related('post_set').all() 
return render(request, 'authors.html', { 
'authors': [{ 
'name': author.name, 
'posts': [p.title for p in author.post_set.all()] 
} for author in authors] 
})

✅ What security measures should you implement in a Django application? 

• Use Django's built-in security middleware 
• Always escape user input in templates ({{ user_input }} auto-escapes) 
• Use Django's CSRF protection 
• Implement proper authentication and authorization 
• Use HTTPS for all communications 
• Secure sensitive settings in environment variables 
• Validate and sanitize all user inputs 
• Limit HTTP methods (decorators like @require_http_methods) 
• Set secure cookie flags 
• Implement rate limiting 
• Keep Django and dependencies updated 


✅  Coding Task: Implement a custom permission class in DRF

from rest_framework import permissions, generics
from .models import BlogPost
from .serializers import BlogPostSerializer

class IsOwnerOrReadOnly(permissions.BasePermission):
    """
    Custom permission to only allow owners of an object to edit or delete it.
    """

    def has_object_permission(self, request, view, obj):
        if request.method in permissions.SAFE_METHODS:
            return True
        return obj.author == request.user

class BlogPostDetail(generics.RetrieveUpdateDestroyAPIView):
    queryset = BlogPost.objects.all()
    serializer_class = BlogPostSerializer
    permission_classes = [permissions.IsAuthenticated, IsOwnerOrReadOnly]


✅  How would you test a Django application? 

• Unit Tests: Test individual components (models, forms, views) 
• Integration Tests: Test how components work together 
• API Tests: Test API endpoints (DRF's APIClient) 
• Client Tests: Test templates and frontend (Django test client) 
• Factory Boy: For creating test data 
• Pytest: Alternative to Django's test runner 
• Coverage.py: Measure test coverage 

✅  Coding Task: Write tests for the BlogPost model ?

from django.test import TestCase 
from django.contrib.auth.models import User 
from .models import BlogPost 
class BlogPostModelTest(TestCase): 
@classmethod 
def setUpTestData(cls): 
test_user = User.objects.create_user( 
username='testuser', password='12345') 
BlogPost.objects.create( 
title='Test Title', 
content='Test Content', 
author=test_user, 
is_published=True 
) 
def test_title_content(self): 
post = BlogPost.objects.get(id=1) 
self.assertEqual(post.title, 'Test Title') 
self.assertEqual(post.content, 'Test Content') 
def test_str_representation(self): 
post = BlogPost.objects.get(id=1) 
self.assertEqual(str(post), 'Test Title') 
def test_published_default_false(self): 
new_post = BlogPost.objects.create( 
title='New Post', 
content='New Content', 
author=User.objects.get(id=1) 
) 
self.assertFalse(new_post.is_published)

✅ How would you deploy a Django application in production ? 

Web Server: Configure Gunicorn or uWSGI 
Reverse Proxy: Set up Nginx or Apache 
Static Files: Configure STATIC_ROOT and collectstatic 
Media Files: Set up proper storage (local or cloud) 
Database: Use production-ready DB (PostgreSQL recommended) 
Environment: Use environment variables for sensitive settings 
WSGI: Configure WSGI application 
HTTPS: Set up SSL certificate 
Monitoring: Implement logging and monitoring 
CI/CD: Set up deployment pipeline

Example Nginx configuration snippet: 

server { 
listen 80; 
server_name example.com; 
location /static/ { 
alias /path/to/static/files; 
} 
location / { 
proxy_pass http://127.0.0.1:8000; 
proxy_set_header Host $host; 
} 
} 
proxy_set_header X-Real-IP $remote_addr;
}
}


✅ Explain Django's architecture ?

Django follows MTV (Model-Template-View) pattern: 
• Model: Database layer (defines data structure) 
• Template: Presentation layer (HTML with Django template language) 
• View: Business logic layer (processes requests and returns responses) 

✅ How do you create a Django project and app? 

// Create project: 
django-admin startproject myproject 

// Create app (inside project directory): 

python manage.py startapp myapp 

// Add app to INSTALLED_APPS in settings.py: 

INSTALLED_APPS = [ 
..., 
'myapp', 
] 

✅ What is a Django migration ? 
Migrations are Django's way of propagating changes you make to your models (adding a field, 
deleting a model, etc.) into your database schema .
Commands: 
• python manage.py makemigrations - Creates migration files 
• python manage.py migrate - Applies migrations to database 

✅ How do you create a superuser in Django?
python manage.py createsuperuser 

✅  What is the Django admin interface?
The Django Admin Interface is a built-in, web-based interface that 
allows developers and authorized users to manage application data
 through a user-friendly UI — without writing any HTML or backend code.

1. Enable admin in urls.py:

from django.contrib import admin
from django.urls import path

urlpatterns = [
    path('admin/', admin.site.urls),
]

2.Create a superuser:
python manage.py createsuperuser

3.Register models in admin.py:

from django.contrib import admin
from .models import BlogPost

admin.site.register(BlogPost)

 ✅ How do you define a model in Django? 
 In Django, a model is a Python class that represents a database table. 
 You define models in the models.py file of your app using 
 Django’s ORM (Object-Relational Mapping).

 from django.db import models
from django.contrib.auth.models import User

class BlogPost(models.Model):
    title = models.CharField(max_length=200)               
    content = models.TextField()                         
    author = models.ForeignKey(User, on_delete=models.CASCADE)  
    created_at = models.DateTimeField(auto_now_add=True)  
    updated_at = models.DateTimeField(auto_now=True)     

    def __str__(self):
        return self.title

 ✅ What are common Django model field types?

 • CharField - For short strings 
• TextField - For large text 
• IntegerField, DecimalField, FloatField - For numbers 
• BooleanField - True/False values 
• DateField, DateTimeField - For dates 
• EmailField - For email addresses 
• ForeignKey - For one-to-many relationships 
• ManyToManyField - For many-to-many relationships 
• FileField, ImageField - For file uploads 

  ✅ How do you handle forms in Django? 

  1. Create a form in forms.py: 

from django import forms 
class ContactForm(forms.Form): 
name = forms.CharField(max_length=100) 
email = forms.EmailField() 
message = forms.CharField(widget=forms.Textarea) 

2. Handle form in view: 

from django.shortcuts import render 
from .forms import ContactForm 
def contact(request): 
if request.method == 'POST': 
form = ContactForm(request.POST) 
if form.is_valid(): 
# Process form data 
return HttpResponse("Thanks!") 
else: 
form = ContactForm() 
return render(request, 'contact.html', {'form': form})

✅ What is Django? What are its advantages?
Django is a high-level Python web framework that follows the "batteries-included" philosophy. 

Advantages: 
• Rapid development 
• Secure by default 
• Scalable 
• Excellent documentation 
• Built-in admin interface 
• ORM for database interactions 
• URL routing system 
• Template engine

 ✅ What is Python? What are its key features?

 Python is a high-level, interpreted programming language known for: 
• Simple and readable syntax 
• Dynamic typing 
• Automatic memory management (garbage collection) 
• Multi-paradigm (OOP, functional, procedural) 
• Extensive standard library 
• Cross-platform compatibility 

✅  What are Python data types?
Common Python data types include: 
• Numbers: int, float, complex 
• Strings: str 
• Sequences: list, tuple, range 
• Mappings: dict 
• Sets: set, frozenset 
• Booleans: bool 
• Binary: bytes, bytearray, memoryview

 ✅ How do you connect Django with a MySQL database?

 1.Install MySQL client:
 pip install mysqlclient

 2. Update DATABASES in settings.py:

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'your_db_name',
        'USER': 'your_username',
        'PASSWORD': 'your_password',
        'HOST': 'localhost',
        'PORT': '3306',
    }
}

3.Run migrations
python manage.py migrate

✅ What are migrations in Django?
Migrations in Django are files that record changes to your models (like adding fields or tables)
and allow Django to apply those changes to the database.

python manage.py makemigrations → Create migration files.
python manage.py migrate → Apply migrations to the database.

✅  How do you perform a Create (Insert) operation in Django ?
You can perform a Create (Insert) operation in Django in two main ways:

🔹 1. Using the Model Directly
from .models import BlogPost

post = BlogPost.objects.create(
    title='New Post',
    content='This is a blog post.',
    author=request.user
)

🔹 2. Using a ModelForm 
form = BlogPostForm(request.POST)
if form.is_valid():
    post = form.save(commit=False)
    post.author = request.user
    post.save()

✅ What is the difference between .get() and .filter() in Django ORM? 

• .get() returns a single object (raises DoesNotExist if not found). 
• .filter() returns a QuerySet (can be empty, but no error). 